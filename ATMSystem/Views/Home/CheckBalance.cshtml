@model Domain.TbClient;

<div class="main-menue" id="main-menue">
    <div class="container">

        <h2 class="title-one text-center mb-4">Check Balance</h2>

        <div class="p-4 border rounded shadow-sm bg-light">

            <div class="form-group">
                <label>Account Number</label>
                <input type="text" value="@Model.AccountNumber" class="form-control" readonly />
            </div>

            <div class="form-group mb-3">
                <label class="fw-bold">Current Balance</label>
                <input type="text" value="@Model.Balance" class="form-control" readonly />
            </div>

            <div class="mt-3">
                <a asp-controller="Home" asp-action="MainMenueScreen" class="btn btn-secondary">Back</a>
            </div>

        </div>

    </div>
</div>
